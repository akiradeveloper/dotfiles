<% if ENV['BEHIND_PROXY'] == '1' %>
# git@github.com ... (SSH using port 22) 
# will be changed into git@ssh.github.com ... (SSH using port 443).
Host github.com
  User git
  HostName ssh.github.com
  Port 443
  #ProxyCommand /usr/bin/corkscrew PROXY_IP PROXY_PORT %h %p /home/akira/.ssh/.authorize
  #ProxyCommand /usr/bin/corkscrew PROXY_IP PROXY_PORT %h %p 
