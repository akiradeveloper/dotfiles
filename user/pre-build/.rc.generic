#!/bin/sh

echo read .rc.generic

# aliases
alias irb='rlwrap irb'
alias ls='ls --color=auto'
alias cl='clear'

if [ -e $HOME/.rc.local ]
then
  . $HOME/.rc.local
else
  echo "~/.rc.local does not exist!"
fi
